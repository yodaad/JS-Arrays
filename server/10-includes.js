// includes returns true if an array contains a specified value, and flase if the value is not found. It is case sensitive.

const pets = ["cat", "dog", "bat"];

const answer = pets.includes("dog");

console.log("Answer: ", answer);
